version: '3.8'

services:
  agent:
    build: .
    environment:
      DATABASE_URL: sqlite:///database_agent.db
      LOG_LEVEL: INFO
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/__pycache__
    command: python -m cli.main --interactive
